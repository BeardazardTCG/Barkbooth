import { prisma } from "@/lib/prisma";import { requireUser } from "@/lib/auth/session";import { Section, Card, ButtonLink } from "@/components/ui";import { reviewProfessionalProfile } from "@/lib/professionals/actions";import { professionalTypeLabels } from "@/lib/professionals/catalog";
export default async function AdminProfessionals(){const user=await requireUser(); if(user.role!=="ADMIN") throw new Error("ADMIN access required."); const profiles=await prisma.professionalProfile.findMany({include:{evidence:true,owner:{select:{displayName:true,username:true}}},orderBy:{updatedAt:"desc"}}); return <Section eyebrow="Admin" title="Review Professional Profiles"><div className="grid gap-4">{profiles.map(p=><Card key={p.id}><div className="flex flex-wrap justify-between gap-3"><div><h2 className="text-xl font-bold">{p.businessName}</h2><p>{professionalTypeLabels[p.type]} · {p.publicationStatus} · {p.verificationStatus} · owner @{p.owner.username}</p><p>{p.evidence.length} evidence items</p></div><ButtonLink href={`/dashboard/professionals/${p.id}`} variant="secondary">Inspect securely</ButtonLink></div><form action={reviewProfessionalProfile} className="mt-3 grid gap-2 md:grid-cols-4"><input type="hidden" name="profileId" value={p.id}/><select name="verificationStatus" defaultValue={p.verificationStatus} className="rounded-2xl p-3"><option value="UNDER_REVIEW">Under review</option><option value="VERIFIED">Verified</option><option value="REJECTED">Rejected</option><option value="NOT_SUBMITTED">Revoke to not submitted</option></select><input name="verificationNotes" placeholder="Owner-visible note" className="rounded-2xl p-3"/><input name="adminReviewNotes" placeholder="Admin review note" className="rounded-2xl p-3"/><button className="button-primary">Save review</button></form></Card>)}</div></Section>}
