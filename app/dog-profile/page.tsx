import { achievements, dogs } from "@/lib/data";
import { Card, PawAvatar, Rosette, Section } from "@/components/ui";

export default function DogProfilePage() {
  const dog = dogs[0];
  return <Section eyebrow="Dog profile" title={dog.name}><div className="grid gap-5 md:grid-cols-[0.8fr_1.2fr]"><div className="space-y-5"><PawAvatar label={dog.name} /><Card><h3 className="text-2xl font-black">{dog.name}</h3><dl className="mt-4 grid gap-3 text-sm"><div><dt className="font-black text-terracotta">Breed</dt><dd>{dog.breed}</dd></div><div><dt className="font-black text-terracotta">Age</dt><dd>{dog.age}</dd></div><div><dt className="font-black text-terracotta">Birthday</dt><dd>{dog.birthday}</dd></div><div><dt className="font-black text-terracotta">Owner</dt><dd>{dog.owner}</dd></div><div><dt className="font-black text-terracotta">Current streak</dt><dd>{dog.streak}</dd></div></dl><button className="mt-5 w-full rounded-full bg-cocoa px-5 py-3 font-black text-white">Share profile</button></Card></div><div className="space-y-5"><Card><p className="text-sm font-black uppercase tracking-widest text-terracotta">Gallery</p><div className="mt-4 grid grid-cols-3 gap-3">{["Garden grin", "Beach ears", "Sofa snooze"].map(item => <div key={item} className="grid aspect-square place-items-center rounded-[1.5rem] bg-biscuit/60 text-center text-xs font-bold text-cocoa/60">📷<br />{item}</div>)}</div></Card><Card><p className="text-sm font-black uppercase tracking-widest text-terracotta">Rosettes won</p><div className="mt-4 flex flex-wrap gap-3">{dog.rosettes.map((r, i) => <div key={r} className="flex items-center gap-2 rounded-2xl bg-biscuit/50 p-2 pr-4 text-sm font-bold"><Rosette label={i === 0 ? "1st" : i === 1 ? "3rd" : "Top"} className={i === 0 ? "bg-amber-300" : i === 1 ? "bg-orange-300" : "bg-skysoft"} />{r}</div>)}</div></Card><Card><p className="text-sm font-black uppercase tracking-widest text-terracotta">Badges earned</p><div className="mt-4 flex flex-wrap gap-2">{achievements.map(b => <span key={b} className={`rounded-full px-3 py-2 text-sm font-bold ${dog.badges.includes(b) ? "bg-terracotta text-white" : "bg-biscuit/60 text-cocoa/55"}`}>🏅 {b}</span>)}</div></Card><Card><p className="text-sm font-black uppercase tracking-widest text-terracotta">Competitions entered</p><div className="mt-4 grid gap-2">{dog.entered.map(c => <span key={c} className="rounded-2xl bg-white p-3 font-bold shadow-soft">{c}</span>)}</div></Card></div></div></Section>;
}
