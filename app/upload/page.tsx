import { competitions, dogs } from "@/lib/data";
import { Card, Section } from "@/components/ui";

export default function UploadPage() {
  return <Section eyebrow="Mock profile activity flow" title="Add an activity entry to a dog profile"><div className="grid gap-5 md:grid-cols-[0.9fr_1.1fr]"><Card><div className="grid h-64 place-items-center rounded-[1.75rem] border-2 border-dashed border-info/30 bg-skysoft/40 text-center"><div><p className="text-5xl">📸</p><p className="mt-3 font-bold">Choose a profile activity photo</p><p className="text-sm text-navy/60">Mock upload only — no file is stored and no entry is submitted.</p></div></div></Card><Card><form className="grid gap-4"><p className="rounded-2xl bg-lightgrey p-3 text-sm font-bold text-charcoal/65">This preview shows how an optional activity could attach to an existing Bark Booth profile later. It does not create uploads, payments, verification, or messages.</p><label className="font-bold">Dog profile<input defaultValue={`${dogs[0].name} · BB-000001`} className="mt-2 w-full rounded-2xl border border-navy/10 bg-offwhite px-4 py-3" readOnly /></label><label className="font-bold">Activity module<select className="mt-2 w-full rounded-2xl border border-navy/10 bg-offwhite px-4 py-3">{competitions.map(c => <option key={c.slug}>{c.title}</option>)}</select></label><label className="font-bold">Activity class<select className="mt-2 w-full rounded-2xl border border-navy/10 bg-offwhite px-4 py-3"><option>Puppy</option><option>Best Rescue</option><option>Action Shot</option></select></label><button type="button" className="rounded-full bg-navy px-5 py-3 font-bold text-white">Preview profile activity</button></form></Card></div></Section>;
}
