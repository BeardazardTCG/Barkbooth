import { Card, Section } from "@/components/ui"; import { reportProblemHref, supportConfig } from "@/lib/launch-config";
export default function SupportPage(){const config=supportConfig();return <Section eyebrow="Help" title="Contact & Support"><div className="grid gap-5 md:grid-cols-2"><Card><h2 className="text-2xl font-bold text-navy">Contact Support</h2><p className="mt-2 text-charcoal/70">Questions about using Bark Booth or your account.</p>{config.supportEmail?<a className="button-primary mt-5" href={`mailto:${config.supportEmail}`}>Contact Support</a>:<p className="mt-5 text-sm text-charcoal/60">The support email is being configured.</p>}</Card><Card><h2 className="text-2xl font-bold text-navy">Report a Problem</h2><p className="mt-2 text-charcoal/70">The report template asks only for troubleshooting details. Do not send passwords, tokens or private records.</p>{config.problemReportEmail?<a className="button-secondary mt-5" href={reportProblemHref(config.problemReportEmail)}>Report a Problem</a>:<p className="mt-5 text-sm text-charcoal/60">The problem-report route is being configured.</p>}</Card>{config.businessEmail&&<Card><h2 className="text-xl font-bold text-navy">Business enquiries</h2><a href={`mailto:${config.businessEmail}`} className="mt-3 inline-flex font-bold text-info">Email Bark Booth</a></Card>}</div></Section>}
