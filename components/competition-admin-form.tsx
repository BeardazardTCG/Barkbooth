import type { Competition } from "@prisma/client";import { saveCompetition } from "@/lib/competitions/actions";import { Card } from "@/components/ui";import { launchPhotoGuidelines } from "@/lib/launch-config";
const local=(d?:Date)=>d?new Date(d.getTime()-d.getTimezoneOffset()*60000).toISOString().slice(0,16):"";
export function CompetitionForm({competition:c}:{competition?:Competition}){return <Card><form action={saveCompetition} className="grid gap-4"><input type="hidden" name="id" value={c?.id??""}/>{[["slug","Slug"],["title","Title"],["theme","Theme"]].map(([name,label])=><label key={name} className="grid gap-1 font-bold">{label}<input required name={name} defaultValue={(c as any)?.[name]??""} className="rounded-2xl p-3"/></label>)}<label className="grid gap-1 font-bold">Description<textarea required name="description" defaultValue={c?.description} className="rounded-2xl p-3"/></label><div className="grid gap-4 md:grid-cols-2"><label className="grid gap-1 font-bold">Opens<input type="datetime-local" required name="opensAt" defaultValue={local(c?.opensAt)} className="rounded-2xl p-3"/></label><label className="grid gap-1 font-bold">Closes<input type="datetime-local" required name="closesAt" defaultValue={local(c?.closesAt)} className="rounded-2xl p-3"/></label></div><label className="grid gap-1 font-bold">Status<select name="status" defaultValue={c?.status??"DRAFT"} className="rounded-2xl p-3">{["DRAFT","PUBLISHED","OPEN","CLOSED","JUDGING","COMPLETED","CANCELLED"].map(x=><option key={x}>{x}</option>)}</select></label><label className="grid gap-1 font-bold">Eligibility<select name="eligibility" defaultValue={c?.eligibility??"UK_ONLY"} className="rounded-2xl p-3"><option value="UK_ONLY">UK only</option><option value="INTERNATIONAL">International</option></select></label><label className="grid gap-1 font-bold">Maximum entries per dog<input type="number" min="1" name="maxEntriesPerDog" defaultValue={c?.maxEntriesPerDog??1} className="rounded-2xl p-3"/></label><label className="grid gap-1 font-bold">Prize summary<textarea required name="prizeSummary" defaultValue={c?.prizeSummary} className="rounded-2xl p-3"/></label><label className="grid gap-1 font-bold">Rules version<input required name="rulesVersion" defaultValue={c?.rulesVersion??"1"} className="rounded-2xl p-3"/></label><label className="grid gap-1 font-bold">Rules<textarea required name="rules" defaultValue={c?.rules} className="min-h-32 rounded-2xl p-3"/></label><label className="grid gap-1 font-bold">Image guidelines<textarea required name="imageGuidelines" defaultValue={c?.imageGuidelines??launchPhotoGuidelines} className="min-h-32 rounded-2xl p-3"/></label><button className="button-primary" type="submit">Save competition</button></form></Card>}
