import {NextResponse} from "next/server";import {prisma} from "@/lib/prisma";import {getObject} from "@/lib/storage";import {getCurrentUser} from "@/lib/auth/session";import {competitionIsPubliclyVisible} from "@/lib/competitions";export async function GET(_:Request,{params}:{params:{competitionId:string}}){const[c,u]=await Promise.all([prisma.competition.findUnique({where:{id:params.competitionId}}),getCurrentUser()]);if(!c?.heroStorageKey||(!competitionIsPubliclyVisible(c.status)&&u?.role!=="ADMIN"))return new NextResponse("Not found",{status:404});const object=await getObject(c.heroStorageKey);return new NextResponse(object.body,{headers:{"content-type":c.heroContentType||"application/octet-stream","cache-control":"public, max-age=300"}})}
