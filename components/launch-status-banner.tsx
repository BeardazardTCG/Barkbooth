import { launchConfig, supportConfig } from "@/lib/launch-config";
export function LaunchStatusBanner() { if (!launchConfig.banner.visible) return null; const support = supportConfig(); return <aside className="rounded-[1.5rem] border border-info/20 bg-skysoft/60 p-5" aria-label="Bark Booth launch status"><p className="font-bold text-navy">{launchConfig.banner.title}</p><p className="mt-1 text-sm leading-6 text-charcoal/70">{launchConfig.banner.message}</p>{support.problemReportEmail && <a className="mt-3 inline-flex font-bold text-info" href="/support">Report a launch problem →</a>}</aside>; }
