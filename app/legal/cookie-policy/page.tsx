import { Card, Section } from "@/components/ui";
export default function CookiePolicyPage() { return <Section eyebrow="Legal" title="Cookie Policy"><Card><p className="leading-7 text-charcoal/70">Bark Booth uses an essential session cookie when you log in so the service can identify your account and protect owner-only areas. It is required for authentication and is not used for advertising. Signing out ends the active server session.</p></Card></Section>; }
