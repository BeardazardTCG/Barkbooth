import { Card, Section } from "@/components/ui";
const questions = [["What is a Bark Booth registry number?", "It is the permanent identifier issued when a dog is registered. It stays attached to that dog’s canonical Bark Booth profile."], ["Who can see a dog’s information?", "Public viewers see only identities intentionally set to public and only their public identity fields. Owners and authorised viewers can access additional records according to their permissions."], ["Can another person help manage a dog?", "Owners can review access requests and grant controlled view, contribution or management access for supported relationships."], ["How do competitions work?", "Published competitions will use registered dog identities. Genuine entries, results, badges and rosettes can then attach to those profiles. No competition is currently open."], ["Can breeders, rescues, fosters and professionals join?", "Yes. Members can apply for the roles already supported by Bark Booth, and approved professionals may appear in the public directory."]];
export default function FAQPage() { return <Section eyebrow="Help" title="Frequently asked questions"><div className="grid gap-4">{questions.map(([q,a]) => <Card key={q}><h2 className="text-xl font-bold text-navy">{q}</h2><p className="mt-2 leading-7 text-charcoal/65">{a}</p></Card>)}</div></Section>; }
