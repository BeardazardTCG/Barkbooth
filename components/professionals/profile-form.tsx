import type { ProfessionalOpeningHour, ProfessionalProfile, ProfessionalProfileService } from "@prisma/client";
import { saveProfessionalProfile } from "@/lib/professionals/actions";
import { supportedLocations } from "@/lib/locations";
import { ServicesFields } from "./profile-form-wrapper";
const input="rounded-2xl border border-navy/10 p-3";
const days=["Monday","Tuesday","Wednesday","Thursday","Friday","Saturday","Sunday"];
type ProfileWithRelations = ProfessionalProfile & { services: ProfessionalProfileService[]; openingHours?: ProfessionalOpeningHour[] };
export function ProfessionalProfileForm({profile}:{profile?:ProfileWithRelations}){ const hours=new Map(profile?.openingHours?.map(h=>[h.dayOfWeek,h])); return <form action={saveProfessionalProfile} className="grid gap-4"><input type="hidden" name="profileId" value={profile?.id??""}/><ServicesFields initialType={profile?.type??"GROOMER"} initialCustomType={profile?.customType} initialServices={profile?.services.map(s=>s.label)??[]} /><label className="grid gap-1 font-bold">Business name<input name="businessName" required maxLength={160} defaultValue={profile?.businessName??""} className={input}/></label><label className="grid gap-1 font-bold">Short description<textarea name="shortDescription" required maxLength={600} defaultValue={profile?.shortDescription??""} className={input}/></label><label className="grid gap-1 font-bold">Year established<input name="yearEstablished" type="number" min="1800" max={new Date().getUTCFullYear()} defaultValue={profile?.yearEstablished??""} className={input}/></label><div className="grid gap-3 md:grid-cols-3"><label className="grid gap-1 font-bold">Email<input name="email" type="email" maxLength={254} defaultValue={profile?.email??""} className={input}/></label><label className="grid gap-1 font-bold">Phone<input name="phone" maxLength={60} defaultValue={profile?.phone??""} className={input}/></label><label className="grid gap-1 font-bold">Website HTTPS<input name="website" type="url" pattern="https://.*" defaultValue={profile?.website??""} className={input}/></label></div><div className="grid gap-3 md:grid-cols-2"><label className="grid gap-1 font-bold">Facebook HTTPS<input name="facebookUrl" type="url" pattern="https://.*" defaultValue={profile?.facebookUrl??""} className={input}/></label><label className="grid gap-1 font-bold">Instagram HTTPS<input name="instagramUrl" type="url" pattern="https://.*" defaultValue={profile?.instagramUrl??""} className={input}/></label></div><div className="grid gap-3 md:grid-cols-2"><label className="grid gap-1 font-bold">Address line 1<input name="addressLine1" maxLength={200} defaultValue={profile?.addressLine1??""} className={input}/></label><label className="grid gap-1 font-bold">Address line 2<input name="addressLine2" maxLength={200} defaultValue={profile?.addressLine2??""} className={input}/></label><label className="grid gap-1 font-bold">Town/city<input name="townCity" maxLength={120} defaultValue={profile?.townCity??""} className={input}/></label><label className="grid gap-1 font-bold">Region/county<input name="regionCounty" maxLength={120} defaultValue={profile?.regionCounty??""} className={input}/></label><label className="grid gap-1 font-bold">Postcode<input name="postcode" maxLength={40} defaultValue={profile?.postcode??""} className={input}/></label><label className="grid gap-1 font-bold">Country<select name="country" defaultValue={profile?.country??"United Kingdom"} className={input}>{supportedLocations.map(c=><option key={c} value={c}>{c}</option>)}</select></label></div><label className="font-bold"><input type="checkbox" name="mobileBusiness" defaultChecked={profile?.mobileBusiness}/> Mobile business</label><div className="grid gap-3 md:grid-cols-3"><label className="grid gap-1 font-bold">Service radius<input type="number" min={1} max={500} name="serviceRadiusValue" defaultValue={profile?.serviceRadiusValue??""} className={input}/></label><label className="grid gap-1 font-bold">Unit<select name="serviceRadiusUnit" defaultValue={profile?.serviceRadiusUnit??"MILES"} className={input}><option value="MILES">Miles</option><option value="KILOMETRES">Kilometres</option></select></label><label className="grid gap-1 font-bold">Public address visibility<select name="addressVisibility" defaultValue={profile?.addressVisibility??"TOWN_AREA_ONLY"} className={input}><option value="TOWN_AREA_ONLY">Town/city and area only</option><option value="SERVICE_AREA_ONLY">Service area only</option><option value="FULL_ADDRESS">Full address</option></select></label></div><fieldset className="rounded-2xl border p-4"><legend className="font-bold">Weekly opening hours (optional)</legend><div className="mt-3 grid gap-3">{days.map((day,index)=>{const h=hours.get(index);return <div key={day} className="grid gap-2 rounded-xl bg-lightgrey p-3 md:grid-cols-[8rem_repeat(5,1fr)]"><label className="font-bold"><input type="checkbox" name={`hours.${index}.closed`} defaultChecked={h?.closed}/> {day} closed</label><input type="time" aria-label={`${day} opens`} name={`hours.${index}.opensAt`} defaultValue={h?.opensAt??""} className={input}/><input type="time" aria-label={`${day} closes`} name={`hours.${index}.closesAt`} defaultValue={h?.closesAt??""} className={input}/><input type="time" aria-label={`${day} second opens`} name={`hours.${index}.secondOpensAt`} defaultValue={h?.secondOpensAt??""} className={input}/><input type="time" aria-label={`${day} second closes`} name={`hours.${index}.secondClosesAt`} defaultValue={h?.secondClosesAt??""} className={input}/></div>})}</div></fieldset><label className="font-bold"><input type="checkbox" name="appointmentOnly" defaultChecked={profile?.appointmentOnly}/> By appointment only</label><label className="grid gap-1 font-bold">Opening hours note<textarea name="hoursNote" defaultValue={profile?.hoursNote??""} className={input}/></label><button className="button-primary">Save draft</button></form> }
