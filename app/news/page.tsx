import {Card,Section,ButtonLink} from "@/components/ui";import {prisma} from "@/lib/prisma";export default async function News(){const posts=await prisma.newsPost.findMany({where:{status:"PUBLISHED",publishedAt:{lte:new Date()}},orderBy:[{featured:"desc"},{publishedAt:"desc"}]});return <Section eyebrow="Bark Booth community" title="News"><div className="mb-5"><ButtonLink href="/events" variant="secondary">View events</ButtonLink></div>{posts.length?<div className="grid gap-4 md:grid-cols-2">{posts.map(p=><Card key={p.id}><p className="registry-label">{p.featured?"Featured news":"News"}</p><h2 className="mt-2 text-2xl font-bold text-navy">{p.title}</h2><p className="mt-3 whitespace-pre-line">{p.summary||p.body}</p><p className="mt-3 text-sm">Published {p.publishedAt?.toLocaleDateString("en-GB")}</p></Card>)}</div>:<Card>No published news yet. Check back for Bark Booth updates, winners and community news.</Card>}</Section>}
