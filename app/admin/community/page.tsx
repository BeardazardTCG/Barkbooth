import {redirect} from "next/navigation";import {requireUser} from "@/lib/auth/session";import {prisma} from "@/lib/prisma";import {Card,Section} from "@/components/ui";import {saveNews,saveEvent,deleteNews,deleteEvent} from "@/lib/community/actions";const dt=(d:Date)=>d.toISOString().slice(0,16);export default async function AdminCommunity(){const u=await requireUser();if(u.role!=="ADMIN")redirect("/dashboard");const[news,events]=await Promise.all([prisma.newsPost.findMany({orderBy:{updatedAt:"desc"}}),prisma.event.findMany({orderBy:{startsAt:"desc"}})]);return <><Section eyebrow="Restricted administration" title="News & Events"><Card><h2 className="text-xl font-bold">Create news</h2><form action={saveNews} className="mt-3 grid gap-3"><input name="title" required placeholder="Title" className="rounded-xl p-3"/><input name="slug" required pattern="[a-z0-9-]+" placeholder="url-slug" className="rounded-xl p-3"/><input name="summary" placeholder="Short summary" className="rounded-xl p-3"/><textarea name="body" required placeholder="News content" className="rounded-xl p-3"/><select name="status" className="rounded-xl p-3"><option value="DRAFT">Save draft</option><option value="PUBLISHED">Publish</option></select><label><input type="checkbox" name="featured"/> Feature item</label><button className="button-primary">Save news</button></form></Card>{news.map(n=><Card key={n.id} className="mt-3"><strong>{n.title}</strong> · {n.status}<form action={deleteNews} className="mt-2"><input type="hidden" name="id" value={n.id}/><button className="button-secondary">Delete</button></form></Card>)}</Section><Section eyebrow="Event operations" title="Manage events"><Card><form action={saveEvent} className="grid gap-3"><input name="name" required placeholder="Event name" className="rounded-xl p-3"/><textarea name="description" required placeholder="Description" className="rounded-xl p-3"/><input name="location" placeholder="Location" className="rounded-xl p-3"/><input type="datetime-local" name="startsAt" required className="rounded-xl p-3"/><input type="datetime-local" name="endsAt" className="rounded-xl p-3"/><input type="url" name="externalUrl" placeholder="https://…" className="rounded-xl p-3"/><input name="organiser" placeholder="Organiser" className="rounded-xl p-3"/><select name="status" className="rounded-xl p-3"><option value="DRAFT">Draft</option><option value="PUBLISHED">Publish</option><option value="ARCHIVED">Archive</option></select><label><input type="checkbox" name="cancelled"/> Mark cancelled</label><button className="button-primary">Save event</button></form></Card>{events.map(e=><Card key={e.id} className="mt-3"><strong>{e.name}</strong> · {e.status} · {dt(e.startsAt)}<form action={deleteEvent} className="mt-2"><input type="hidden" name="id" value={e.id}/><button className="button-secondary">Delete</button></form></Card>)}</Section></>}
